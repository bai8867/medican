# C-1 路线：未完成事项

> 背景：你已选择 **C（系统治理）** 下的 **C-1（安全与稳定优先）** 路径。下列为 **尚未在本轮落地** 或 **仅部分覆盖** 的工作，便于后续迭代与验收。

---

## 已完成（便于对照，非待办）

- **前端 B（部分）**：`interceptors` 对账号禁用（4031）的处理改为 **`accountDisabledNavigator` + `main.js` 注入回调**，消除对 `user` / `adminAuth` / `router` 的 `import()`，Vite 不再报「动态与静态导入并存无法分包」告警；`vite.config.js` 增加 **`vendor-vant`**，`vendor-misc` 体积下降。
- 学生管理接口角色提权封堵（`AdminUserController`）及对应单测。
- 默认安全基线收紧（如 `server.address`、`application.yml` 中与默认账号/监听相关的调整，以仓库当前版本为准）。
- 仓库忽略本地工具目录（`.gitignore`）。
- `AuthService` / `AdminUserController` 等关键单测（以仓库内 `src/test` 为准）。
- **鉴权链路单测（第二批 A）**：`JwtAuthFilterTest`、`AuthControllerTest`。
- **管理端契约单测**：`AdminSystemControllerApiContractTest`、`AdminCampusWeeklyCalendarControllerApiContractTest`（系统 KV、周历保存/入参等）。
- **仓库卫生**：根目录 **`/uv.exe`** 已写入 **`.gitignore`**；**`npm audit fix`** 已处理可自动修复项（`follow-redirects`）；`xlsx` 高危项无官方修复路径，见 **`README.md` 常用命令** 说明。
- **CORS / JWT 过滤器顺序**：**`WebConfig`** 将 **`CorsFilter`** 与 **`JwtAuthFilter`** 均注册为 **`FilterRegistrationBean`**，显式 **`Ordered.HIGHEST_PRECEDENCE`**（CORS）先于 JWT；数值契约见 **`WebConfigFilterOrderContractTest`**；**`CorsJwtPreflightMockMvcTest`** 在 **MockMvc** 上按相同顺序挂载两过滤器并验证 **`OPTIONS /api/health`** 的 CORS 响应头（无 `Authorization`）。与嵌入式 Tomcat 全链相比仍有环境差异，但已覆盖典型浏览器预检路径。
- **CI 前端 E2E 门禁**：**`npm run test:e2e:ci`**（`scripts/playwright-ci-gate.mjs` + `e2e/ci-gate.spec.js`，**build 后** vite preview **11998** 端口，不依赖后端）已接入 **`.github/workflows/ci.yml`**。
- **文档**：**`README.md`** 已含主路线索引、Wrapper 说明、audit/xlsx 说明；**`AGENTS.md`** 已注明 Wrapper 提交形态。

---

## 未完成事项

### 1. 前端性能与构建（原「B」批次）

| 项 | 说明 | 建议验收 |
|----|------|----------|
| 生产构建体积与拆包 | `element-plus` / `echarts` 等 vendor 仍可能 >700kB（告警阈值见 `vite.config.js`） | 可选：`unplugin-vue-components` + Element 按需、ECharts 按图表子包裁剪；或接受阈值/懒加载现状 |
| `request.js` 与 store/router 的耦合 | ~~动态 `import()` 与静态 import 并存~~ → **已用入口注入回调处理 4031** | 若以后再从 http 层直接触达 Pinia/Router，保持「仅调已注册回调」模式 |
| 可选：Element Plus / ECharts 等大依赖按需引入 | 若全量打入主 chunk，体积难降 | 审计 `main.js` 与全局 CSS 引入方式 |

### 2. 后端可复现构建与 CI

| 项 | 说明 | 建议验收 |
|----|------|----------|
| Maven Wrapper（`mvnw` / `mvnw.cmd`） | ~~无全局 Maven 时难以一键跑测~~ → **已随仓库提交**（见 `AGENTS.md`） | 新环境仅 JDK 即可 **`./mvnw` / `mvnw.cmd test`** |
| CI 流水线 | ~~可选~~ | 已跑前端 lint/typecheck/unit/**e2e:ci**/build 体积门禁 + 后端 **`mvnw test`** + 安全基线脚本 |

### 3. 测试覆盖缺口（在已有单测之外）

| 项 | 说明 | 建议验收 |
|----|------|----------|
| `JwtAuthFilter` 与真实 `FilterRegistrationBean` 顺序集成 | **`WebConfig`** 已显式注册 CORS/JWT 及 order；**`WebConfigFilterOrderContractTest`** + **`CorsJwtPreflightMockMvcTest`**（OPTIONS 预检头） | 若仍要 **嵌入式 Tomcat 全应用上下文** 下的 OPTIONS（含其它过滤器/静态资源），可再加 **`@SpringBootTest` + `MockMvc`**（需 H2/测试 profile 等） |
| 更多管理端 / 校园端 Controller | 以 MockMvc + 权限桩扩展 | 对高风险路径（用户禁用、系统 KV、周历发布等）逐条补测 |
| 前端 E2E | **CI 门禁**：`npm run test:e2e:ci`（静态路由）；**全链路**：登录/推荐/体质等仍依赖后端，本地 `npm run test:e2e` | CI 绿 + 本地全量 e2e 按需跑通 |

### 4. 依赖与安全扫描

| 项 | 说明 | 建议验收 |
|----|------|----------|
| `npm audit` | ~~registry~~ | 已用官方 registry 执行 **`npm audit fix`**；**GitHub Actions** 前端 job 已跑 **`npm audit --audit-level=critical`**（本地等价 **`npm run audit:ci`**）。**`xlsx`** 仍常为 **high** 且无安全版本，见 `README.md` |

### 5. 文档与流程（可选，偏「完整 C」）

| 项 | 说明 |
|----|------|
| 设计/计划落盘 | 若团队规范要求 `docs/superpowers/specs` 与 `plans`，可补「系统治理」专题 spec/plan |
| README 联动 | ~~`SERVER_ADDRESS`、`MYSQL_*`、种子与 CORS 等~~ → **`README.md` 快速启动节「环境变量速查」** |

### 6. 仓库卫生（与功能无关但影响协作）

| 项 | 说明 |
|----|------|
| 大量未跟踪/未提交文件 | **`/uv.exe`** 已忽略；其余 `.agents/`、大 zip 等仍建议按需归档或忽略 |
| 与工作项无关的进行中改动 | 合并前建议分批提交，便于 review 与回滚 |

---

## 建议执行顺序（未完成部分）

1. ~~**前端 B**：拆包 + `request.js` 解耦~~（部分已完成，见上文「已完成」）。  
2. ~~**`mvnw` + 文档**~~（已对齐 README / AGENTS / CI）。  
3. ~~**E2E 门禁 / npm audit 可自动修复项**~~（见上文）；剩余：**过滤器与真实装配顺序集成测**、**Element/ECharts 按需**、**xlsx 替换或接受风险**、**README 种子账号等细项**。

---

## 变更说明

- 本文档路径：`docs/C-1-未完成事项.md`  
- 仅描述「未完成」与建议验收方式；具体实现以当时代码为准。
