# 路径2未完成项清单（方案B-渐进落地）

## 1. 适用范围与当前基线

本文档仅针对“路径2（B-渐进落地）”已执行后的剩余工作，不覆盖仓库内其它并行改动。

当前已落地的关键基线（简述）：
- 已补最小 CI 骨架：前端检查 + 后端测试（后端 job 使用 `./mvnw test`，与 Wrapper 对齐）流程已写入。
- 前端已升级 **2B 类型门禁**：`typecheck` 使用 `vue-tsc --noEmit`（`typecheck:legacy` 保留 `tsc --noEmit --noCheck` 应急）；`lint`、`test`、`build` 不变。
- 前端 `Recommend.vue` 已做第一、二刀拆分：偏好持久化与筛选排序纯逻辑已抽离。
- 后端 `CampusWeeklyCalendarService` 已完成首刀：菜品富化职责拆到 `CampusWeeklyCalendarRecipeEnricher`，并补了对应单测文件。

**智能体本轮（2026-04-16）已执行的非人工闭环**：
- 后端：`scripts/Run-BackendTests.ps1` → **172 tests，BUILD SUCCESS**（以本机 Surefire 汇总为准）。
- 前端：`tcm-diet-frontend` 下 **`npm run typecheck`**（`vue-tsc --noEmit`）、**`npm run lint`**、**`npm run test`**（`tsx --test`）**通过**。
- **P1-1（深化）**：`src` 下 **`data/`、`stores/`、`utils/`、`composables/`、`mocks/`、`router/index.ts`、`main.ts`** 已由 `.js` 迁移为 `.ts`（`index.html` 入口同步为 `main.ts`；`vite-plugins/*.js` 仍为 JS）；迁移民模块首行使用 **`// @ts-nocheck`** 以保持与历史 JS 等价的检查范围，Api 层通过 **`MockUnifiedRecipeRow`**（`src/api/mockTypes.ts`）收窄 Mock 回调参数类型；**`eslint.config.js`** 接入 **`@typescript-eslint/parser`** 并 **`ignores: src/**/*.d.ts`**；**`package.json`** 增加 **`tsx`**，`sync:backend-mock-json` 与 **`test`** 脚本已对齐 `.ts`。
- **P1-4**：**`docs/api-contract-openapi-snippet.yaml`** 已增补 **`POST /api/auth/login`**、**`GET /api/campus/recipes/recommend-feed`** 及对应 `components.schemas`；**`docs/api-contract.md`** 链入关系不变。
- **P2-2**：在现有 **`scripts/Fetch-ObservabilitySummary.ps1`** 上增加可选阈值门禁（超阈 **exit 5**）；**`docs/observability-dashboard-playbook.md`** 与 **`docs/observability-metrics.md`** 同步说明；指标文档中「§」符号已改为「第 X 节」表述以改善渲染。
- **安全基线脚本**：本机 Cursor 环境 **未安装 `bash`** 时无法本地执行 `check-prod-security-baseline.sh`；以 **CI / Git Bash / WSL** 执行 **exit 0** 为准（与清单 P0-2 原述一致）。

---

## 2. 未完成项总览（按优先级）

## P0（必须先完成）

### P0-1 后端测试闭环未跑通（环境阻塞）
- **现状**：仓库已含 **`campus-diet-backend` Maven Wrapper**（`mvnw.cmd`）；`启动后端.bat` 已支持 **`.devtools` / `dev-tools`** 与无全局 `mvn` 时回退 Wrapper；根目录 **`scripts/Use-RepoJavaMaven.ps1`** 便于 Cursor 终端注入 JDK/Maven。**清单原「无 mvnw」描述已过时。**
- **缺口**：CI 与多人环境需持续保持 **`./mvnw` + JDK 11** 与 Surefire 3.x 一致；**P0-2 安全基线**仍为独立项。
- **补充（2026-04）**：本地已可 **`.\mvnw.cmd -B -ntp test` 全绿**；曾修复 `RecommendQueryRepository` 缺少 `Set` 导入、以及 Windows 下 Surefire 2.22 fork 类路径过长问题（`pom.xml` 显式 **`maven-surefire-plugin` 3.2.5**）。**本轮复验：Run-BackendTests → 172 tests 通过。**
- **下一步**：
  1. ~~统一后端本地执行方式（二选一）：安装 Maven 或补 `mvnw`。~~（已完成：优先 `.\mvnw.cmd`，可选便携目录 + 脚本。）
  2. ~~在本地跑通后端测试。~~（推荐：仓库根 **`powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\Run-BackendTests.ps1`**；等价于 `Use-RepoJavaMaven` + `mvnw` + `maven-settings-d.xml`。）
  3. **责任人**：在 GitHub 上对当前分支完成 **push + PR** 各一次，确认 **Backend Tests** job **连续两次绿灯**（智能体无法代点远程 CI）。
- **验收标准**：
  - 本地可执行后端测试命令并通过；
  - CI 后端 job 连续 2 次通过（push + PR）。
- **负责人**：后端负责人（待填）
- **建议时长**：0.5 ~ 1 天
- **依赖**：Java 11 与 Maven 运行环境

### P0-2 安全基线“可验收化”未完成
- **现状（已更新）**：已落地可执行清单 `docs/security-baseline-checklist.md`；`prod` 下 `ProdSecurityBaselineValidator` 启动阻断；CI 中已执行 `scripts/check-prod-security-baseline.sh`；并有 `ProdSecurityBaselineValidatorTest`。
- **缺口**：发布流程（非 GitHub Actions）侧是否同步执行静态脚本，需运维确认；演练记录是否写入运维台账由团队执行。
- **下一步**：
  1. **自动化证据**：已增加演练留档示例 **`docs/security-baseline-drill-logs/2026-04-16-p0-closeout.md`**（含 `ProdSecurityBaselineValidatorTest` / 全量 `mvn test` 命令摘要）；清单正文 **`docs/security-baseline-checklist.md` 第 5 节**已指向该目录。**仍须人工**：按第 **4** 节步骤 1～3 做 **prod 进程级**故意违规启动与恢复，并回填留档表。
  2. 在 **Git Bash / WSL / Linux** 或 CI 执行 **`bash scripts/check-prod-security-baseline.sh`**（本机无 `bash` 时以 CI 或上述环境为准），将 **exit 0** 输出记入台账或留档文件。
  3. 若有独立发布流水线，将 `check-prod-security-baseline.sh` 接入为发布前门禁。
  4. 按需扩展清单字段（新密钥、新开关）时同步更新脚本 `grep` 断言与校验器。
- **验收标准**：
  - 安全清单文档存在并可被执行；
  - 生产配置缺失时能阻断启动或发布；
  - 至少一次演练证明阻断有效。
- **负责人**：后端负责人 + 运维（待填）
- **建议时长**：1 天
- **依赖**：发布流程权限

---

## P1（本周应推进）

### P1-1 前端类型门禁仍是过渡方案（2A）
- **现状（已更新）**：`npm run typecheck` 使用 **`vue-tsc --noEmit`**；**`src/api`**、**`src/data`**、**`src/stores`**、**`src/utils`**、**`src/composables`**、**`src/mocks`**、**`src/router`**、**`src/main.ts`** 均为 **`.ts`**（业务侧 **`src` 内已无 `.js` 源文件`**；构建插件 **`vite-plugins/*.js`** 除外）。迁移民模块采用 **`// @ts-nocheck`** + Api 层 **`MockUnifiedRecipeRow`** 的组合以保持 CI 可绿；**`eslint`** 已用 **`@typescript-eslint/parser`** 覆盖 **`src/**/*.{js,ts,...}`**。**本轮：`typecheck` + `lint` + `test` 均通过。**
- **缺口（技术债）**：按目录**逐步移除 `@ts-nocheck`** 并补全显式类型（优先 **`composables/`** 与 **`stores/`**）；**`eslint-plugin-vue`** 等 SFC 专项规则仍未接入；**`vite-plugins`** 若需类型检查可后续改为 `.ts` 或单独子工程。
- **下一步**：
  1. ~~切换至 2B 方案（`vue-tsc --noEmit`）。~~（已完成基线。）
  2. ~~`src/api` 与高频目录 `.ts` 化~~（已完成 **`data` / `stores` / `utils` / `composables` / `mocks` / 路由与入口** 迁移；后续为**去 `@ts-nocheck` + 收紧隐式 any** 排期。）
  3. ~~将“类型错误数”纳入每周收敛指标~~：`npm run typecheck:report` / `npm run typecheck:count`（同脚本，见仓库根 `scripts/count-vue-tsc-errors.mjs`）；可选设 `TCM_TYPECHECK_REPORT_ONLY=1` 仅打印计数。
- **验收标准**：
  - CI 使用真实类型检查命令；
  - 首批高频目录类型错误清零；
  - 新增代码不得引入新的类型错误。
- **负责人**：前端负责人（待填）
- **建议时长**：1.5 ~ 2 天
- **依赖**：前端统一规范决策

### P1-2 前端 UI 双栈治理未完成
- **现状**：项目仍存在双 UI 栈并行使用。
- **缺口（已部分补齐）**：策略与评审项已写入 **`docs/ui-stack-strategy.md`**，PR 模板含 UI 栈自检；**代码层混用治理**仍按域渐进。
- **下一步**：
  1. ~~明确 UI 主栈与允许的例外场景。~~（见 `docs/ui-stack-strategy.md`。）
  2. ~~建立新增组件准入规则（代码评审检查项）。~~（见 PR 模板。）
  3. ~~从高频页面开始替换明显混用点（示例）~~：`CampusLogin.vue`、`EntrySelect.vue`、`AccountDisabled.vue`、`Constitution.vue`、`NotFound.vue`、`AIPlanDetail.vue`、`AIGenerate.vue` 已改为 **Vant**（按钮 / 表单域 / `showToast` / 导航栏）；其它校园页按需排期。
- **验收标准**：
  - 有书面策略（主栈、例外、迁移顺序）；
  - 新增页面不再引入双栈混用；
  - 完成至少 1 个高频页面治理示例。
- **负责人**：前端负责人（待填）
- **建议时长**：1 ~ 2 天
- **依赖**：设计/前端协同结论

### P1-3 后端分层边界仍需继续拆分
- **现状**：仅完成周历富化首刀，服务层仍有多处“大类聚合职责”。
- **缺口（已部分推进）**：**AI 食疗**侧已抽出 **`AiTherapyPlanLlmPromptBuilder`**、**`AiTherapyPlanOutputMerger`**、**`AiTherapyPlanQualityGovernance`**、**`AiTherapyPlanLlmInvocation`**，并由 **`AiTherapyPlanGenerationOrchestrator`** 编排；**`AiTherapyPlanService`** 现为薄门面。可再按域升域或继续压缩 orchestrator 体积。
- **下一步**：
  1. ~~选 1 个高耦合服务做第二刀拆分（AI 相关）。~~（首刀：提示词构建；可继续拆 merge/quality。）
  2. ~~明确分层命名与依赖方向~~：见 **`docs/backend-layering.md`**（`service.ai` 包内继续沉淀或升域时以此为准）。
  3. ~~同步补最小测试护栏~~（已加 Builder / Merger / Sanitizer 等单测；回归以 `AiTherapyPlanServiceTest` 与全量 `mvn test` 为准；**本轮全量通过**）。
- **验收标准**：
  - 第二个长服务拆分完成并可独立测试；
  - 无循环依赖或跨层反向引用；
  - 关键功能回归通过。
- **负责人**：后端负责人（待填）
- **建议时长**：2 天
- **依赖**：P0-1 测试环境可用

### P1-4 API 规范统一尚未成文
- **现状（已更新）**：已新增 **`docs/api-contract.md`**（命名、分页、`ApiResponse`、兼容原则、核心流索引）；**`.github/pull_request_template.md`** 含 API 与 UI 栈自检项；后端契约样例覆盖全部 **`controller` 包内 `*Controller.java`**（各域 `*ApiContractTest`）。**`docs/api-contract-openapi-snippet.yaml`** 已含：**`/api/health`**、**`/api/admin/dashboard/observability-summary`**、**`POST /api/auth/login`**、**`GET /api/campus/recipes/recommend-feed`** 及 **`ApiResponse` 包络相关 schema**；**`api-contract.md`** 已引用；核心流表格已与 **`profile.ts` / `ai.ts`** 对齐。
- **缺口**：全量 OpenAPI / JSON Schema 仍非单文件生成物；可按域继续扩展 snippet 或引入 **springdoc** 等工具链（参见业界 contract-first / springdoc 实践）。
- **下一步**：
  1. ~~定义 API 契约最小规范。~~（见 `docs/api-contract.md`。）
  2. ~~为核心 API 增加契约检查样例（首批：校园推荐流）。~~（已扩展至全部业务 Controller 契约测试；按需为 Actuator 等例外路径单独立项。）
  3. ~~纳入 PR 检查清单。~~（见 PR 模板。）
  4. ~~（可选）扩展 **`api-contract-openapi-snippet.yaml`** 中高频路径~~（已增登录与校园推荐 feed；评审 diff 关注与 breaking-change 检测仍属持续项）。
- **验收标准**：
  - 规范文档落地并被引用；
  - 核心 API 契约样例可执行；
  - PR 模板中包含契约检查项。
- **负责人**：后端负责人 + 前端负责人（待填）
- **建议时长**：1 天
- **依赖**：跨端协同评审

---

## P2（持续优化）

### P2-1 构建与包体门禁（初版已落地）
- **现状（已更新）**：CI 前端 job（见 `.github/workflows/ci.yml`）在 **`build` 步骤**执行 **`scripts/run-frontend-build-with-timing.sh`**：打印 **`FRONTEND_BUILD_SECONDS`**，并在 **`FRONTEND_BUILD_GUARD=1`** 下以 **`FRONTEND_BUILD_MAX_SECONDS`** 超限**阻断**；随后在 **`dist` 已生成**的前提下执行 **`scripts/report-frontend-dist-size.sh`**，以 **`FRONTEND_DIST_GUARD=1`** 与体积上限环境变量**阻断**。
- **缺口**：构建/体积数据的**趋势看板**与**告警分层**仍未建；`dist` 与 `FRONTEND_BUILD_MAX_SECONDS` **基线需随大版本或拆包收益定期下调**，避免门禁形同虚设。
- **下一步**：
  1. ~~建立包体与构建时长基线并输出到 CI 日志~~（包体：`DIST_*`；时长：`FRONTEND_BUILD_SECONDS`）。
  2. ~~设置体积与构建耗时阈值并由 workflow 配置~~（`FRONTEND_DIST_*` / `FRONTEND_BUILD_MAX_SECONDS`）。
  3. ~~接入 CI 失败阈值（阻断）~~（体积与构建均已接入）。
  4. ~~择一迭代：将 Action 摘要或脚本输出**归档为可对比的构建记录**（artifact / 注释基线）~~：**已落地** CI `frontend-ci-metrics` artifact（`build-with-timing.log`、`dist-size-report.log`）；操作说明见 **`docs/frontend-ci-metrics-playbook.md`**，本地对照脚本 **`scripts/Compare-FrontendCiMetrics.ps1`**。**仍须人工**：完成至少一次「超阈定位 → 拆包或调参 → 验证收紧」闭环后下调阈值。
- **验收标准**：
  - 有可追踪的基线数据；
  - 超阈值时有明确告警/阻断；
  - 完成至少一次优化闭环（定位 -> 修复 -> 验证）。
- **负责人**：前端负责人（待填）
- **建议时长**：1 天（初版）

### P2-2 可观测性未形成统一看板
- **现状**：已有改进方向，但缺少统一指标面板与告警分层。
- **缺口（已部分补齐）**：已新增指标说明 **`docs/observability-metrics.md`**（5 项核心指标与计数器命名）；`RuntimeMetricService.snapshot()` 增加 **`maxCostMs`**；推荐 Feed 增加 **`recommend.feed.page_nonempty` / `page_empty`**；管理端新增 **`GET /api/admin/dashboard/observability-summary`**。**本轮：`docs/observability-dashboard-playbook.md` 与 `Fetch-ObservabilitySummary.ps1` 已覆盖「定时拉取 + 可选阈值门禁（exit 5）」；指标文档用语已统一。仍须人工**：商业 Grafana/告警路由与一次故障演练。**
- **下一步**：
  1. ~~确认关键指标（错误率、耗时、推荐成功率、AI 调用失败率）~~：见 **`docs/observability-metrics.md`**。
  2. ~~补齐埋点与日志字段~~：与 **`RequestObservabilityInterceptor`** / **`RuntimeMetricService`** 对齐；推荐见上。
  3. ~~建立最小看板与告警策略（拉取 `observability-summary` 或后续 Prometheus）~~：**文档 + 脚本门禁已落地**；后续接 Prometheus / 云 APM 仍属扩展项。
- **验收标准**：
  - 核心指标可查询；
  - 高优先级告警可触达；
  - 至少完成一次故障演练。
- **负责人**：后端负责人 + 运维（待填）
- **建议时长**：1.5 天（初版）

---

## 3. 推荐执行顺序（下一周）

1. **D1**：完成 P0-1（测试环境与后端测试闭环）  
2. **D1-D2**：完成 P0-2（安全基线验收化）  
3. **D2-D3**：推进 P1-1（去掉 `@ts-nocheck`、收紧隐式 any；可选 `eslint-plugin-vue`）  
4. **D3-D4**：推进 P1-3（后端第二刀拆分）  
5. **D4**：P1-4 持续扩展 OpenAPI snippet / springdoc；P1-2（UI 双栈整页迁移，如 **Profile**）  
6. **D5**：P2-1 后续（阈值收紧、构建记录归档）与 P2-2 可观测性初版指标预研

---

## 4. 执行检查清单（每次迭代结束前）

- 是否所有“本周新增”都可被自动化验证（CI / test / lint / typecheck）？
- 是否新增了未纳入规范的接口字段或返回结构？
- 是否出现了跨层调用反向依赖？
- 是否留下“已知问题但无 issue/负责人/截止时间”的隐性债务？

若任一项为“是”，不得进入下一批重整任务，需先补齐闭环。
