# 安全基线演练留档（P0-2 / 清单第 5 节）

> 对应 `docs/security-baseline-checklist.md` 第 4～5 节。每次演练可复制本表新开一条；本文件为 **2026-04-16 自动化证据 + 待人工补全项**。

## 演练记录表

| 字段 | 填写 |
|------|------|
| 演练日期 | 2026-04-16 |
| 执行人 | 研发侧自动化验证（Cursor 代理会话）；**人工阻断演练执行人待填** |
| 环境 | 本机 / CI 等价：`campus-diet-backend` + `prod` profile **单测**（非完整 Spring Boot 进程） |
| 违规项（故意配置） | **未执行** 清单第 4 节步骤 1～3（`spring.datasource.username=root` 等启动阻断）；须在具备 prod 配置与数据库隔离的环境由负责人补做 |
| 启动结果 | 待人工：预期启动失败并记录 `IllegalStateException` 首行 |
| 异常摘要 | 待人工 |
| 恢复后启动 | 待人工 |
| `check-prod-security-baseline.sh` | **本机未检出 `bash`**：未跑。请在 **Git Bash / WSL / Linux** 于仓库根执行 `bash scripts/check-prod-security-baseline.sh`，预期输出末行 `[security-check] prod security baseline checks passed.` 且 **exit 0**；**GitHub Actions** 已在 `ci.yml` 后端 job 中执行该脚本 |
| `ProdSecurityBaselineValidatorTest` | **已通过**（见下方命令与证据摘要） |
| 备注 | 非 GitHub 发布流水线是否已接入同一脚本：□ 是 □ 否 / N/A（待运维勾选） |

## 自动化证据摘要（可粘贴至台账）

```text
命令（仓库根，PowerShell）:
  powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\Run-BackendTests.ps1 -SecurityOnly

结果: BUILD SUCCESS；ProdSecurityBaselineValidatorTest Tests run: 3, Failures: 0

命令（同上，全量后端测试）:
  powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\Run-BackendTests.ps1

结果: BUILD SUCCESS；全量 Tests run: 51, Failures: 0（以当时仓库为准）
```

## 待办（关闭 P0-2 审计缺口）

1. 在 **prod 或类 prod** 环境完成清单 **第 4 节步骤 1～3** 的故意违规与恢复，并回填上表「违规项 / 启动结果 / 异常摘要」。
2. 在可执行 **bash** 的环境跑一次 `check-prod-security-baseline.sh`，将终端输出存档或粘贴至运维台账。
3. 若有独立发布流水线，将 `scripts/check-prod-security-baseline.sh` 接入发布前门禁，并在上表「备注」勾选。
