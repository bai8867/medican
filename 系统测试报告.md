# 校园药膳推荐系统 — 系统测试报告

- **生成时间**: 2026-04-13 00:56:39
- **仓库根目录**: `D:\HuaweiMoveData\Users\baide\Desktop\medican`

## 1. 测试范围

| 层级 | 内容 |
| --- | --- |
| 前端 | `tcm-diet-frontend`：`npm test`（体质问卷算法）；开发态可用 `npm run dev`（默认 **11999**，Vite 代理 `/api` → 后端） |
| 后端 API | 根目录 `test_integration.py`（健康检查、登录、体质、推荐、AI、收藏、历史、设置、场景、管理端） |
| 未纳入 | 后端暂无 `src/test` 单测；Playwright E2E 未默认执行 |

## 2. 运行环境与前后端就绪（探测）

本节前执行一次 HTTP 探测，用于说明**测试执行时**前后端是否可达（与 `npm test` 无必然关系）。

| 探测项 | URL | 结果 |
| --- | --- | --- |
| 后端健康检查 | `http://127.0.0.1:11888/api/health` | **可达** — HTTP 200，正文前 80 字: '{"status":"ok"}' |
| 前端开发服务（可选） | `http://127.0.0.1:11999/` | **可达** — HTTP 200，正文前 80 字: '<!DOCTYPE html>\r <html lang="zh-CN">\r   <head>     <script type="module" src="/@' |

环境变量：`BASE_URL`（默认 `http://127.0.0.1:11888`）、`FRONTEND_CHECK_URL`（默认 `http://127.0.0.1:11999/`）。

## 3. 前端单元测试（`npm test`）

**退出码**: `0` — **结论**: 通过

```text
✔ 平和质特殊：8 偏颇均≤3 且第9题≥4 → 主导平和 (1.0375ms)
✔ 平和质特殊：边界全为 3 与 4 (1.0245ms)
✔ 不满足特殊：任一偏颇题>3 则不走平和强制，按最高分+并列顺序 (0.2334ms)
✔ 不满足特殊：第9题<4 时按最高分并列（全3时气虚质优先） (0.2579ms)
✔ 并列最高分：气虚与平和同 5，无平和特殊时取气虚 (0.2796ms)
✔ 未答完或非法分值：平和特殊为 false，仍按已填题计分 (0.2042ms)
✔ 阈值可覆盖：放宽偏颇上限后满足平和特殊 (0.2334ms)
✔ 平和特殊可覆盖「全项同分并列」：阈值为 3 时九题皆 3 → 强制平和而非气虚 (0.1939ms)
ℹ tests 8
ℹ suites 0
ℹ pass 8
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 167.8399
```

## 4. 后端 API 联调（`test_integration.py`）

**退出码**: `0`（非 0 表示存在失败用例或无法连接服务）。

### API 联调明细（自动生成）

- **生成时间**: 2026-04-13 00:56:41
- **BASE_URL**: `http://localhost:11888`
- **通过 / 失败 / 警告**: 22 / 0 / 2

## 用例明细

| 结果 | 用例 | 详情 |
| --- | --- | --- |
| 通过 | 健康检查 | JSON status=ok |
| 通过 | 用户登录 | path=/api/auth/login |
| 通过 | 问卷判定气虚质 | {'primaryLabel': '气虚质', 'scores': {'pinghe': 2, 'qixu': 5, 'yangxu': 2, 'yinxu': 2, 'tanshi': 2, 'shire': 2, 'xueyu': 2, |
| 通过 | PUT 用户资料更新体质 | /api/user/profile |
| 通过 | Profile 体质为 yinxu |  |
| 通过 | 推荐列表非空 | n=12 path=/api/campus/recipes/recommend-feed |
| 通过 | 推荐卡片类型 | 含体质匹配与 AI 入口标记 |
| 警告 | 季节参数 | 冬/夏推荐列表 id 序列相同，可能池子小或排序巧合 |
| 警告 | AI 响应时间 | 63ms < 500ms，疑似 Mock 或未走真实大模型 |
| 通过 | AI 字段完整性 | 后端等价字段齐全 |
| 通过 | AI 内容变化检测 | 两次返回不同 |
| 通过 | 荔枝/上火语义 | 命中: 荔枝,清热 |
| 通过 | 收藏添加 | recipe_id=1 |
| 通过 | 收藏列表含该 ID | 1 |
| 通过 | 取消收藏 | DELETE /api/user/favorites/{id} |
| 通过 | 药膳详情 | /api/recipes/1 |
| 通过 | 浏览历史 | 含 recipeId=1 |
| 通过 | 获取用户设置/资料 | settings 或 profile |
| 通过 | 关闭个性化推荐 | recommend_enabled / recommendEnabled |
| 通过 | 设置已持久化 | recommendEnabled=false |
| 通过 | 场景数量 | 10 |
| 通过 | 场景药膳 | scene 1 recipes=3 |
| 通过 | 管理员新增药膳 | id=39 |
| 通过 | 看板字段(后端) | topCollected + seasonalPicks（与 PRD 命名不同） |

## 5. 总评

- 运行探测：后端 `/api/health` **正常**；前端开发地址 **正常**（仅作环境说明）。

- 前端单元测试：**通过**。
- 后端 API 联调：**通过**（在当前 `BASE_URL` 环境下）。

> 说明：联调中的 AI、大模型相关用例可能产生 **警告**（如响应过快、两次结果相同）， 不代表接口失败；以报告中「失败」行为准。
